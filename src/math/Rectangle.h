#pragma once

#include "math/Vec2.h"

class Rectangle{
public:
    Rectangle() = default;
    Rectangle(float a, float b, float x, float y);

    //Rectangle(Vec2f a_pos, Vec2f a_right, Vec2f a_top);

    //Vec2f pos, right, top;
    float c,d,q,r;
private:
};
